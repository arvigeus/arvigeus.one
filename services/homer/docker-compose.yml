services:
  homer:
    image: b4bz/homer:v25.09.1
    container_name: homer
    restart: unless-stopped
    hostname: homer
    environment:
      - TZ=${TZ}
    volumes:
      - ${DATA}/homer:/www/assets:ro

networks:
  default:
    external: true
    name: ${DOCKER_NETWORK}
