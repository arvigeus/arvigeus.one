opencode.{$DOMAIN} {
	reverse_proxy opencode:4096
}

# Vite dev server runs on port 5173 inside the opencode container.
# Run `npm start -- --host 0.0.0.0` or set server.host in vite.config.ts
preview.{$DOMAIN} {
	reverse_proxy opencode:5173
}
