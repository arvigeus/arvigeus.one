services:
  memos:
    image: neosmemo/memos:0.29.1
    hostname: memos
    container_name: memos
    restart: unless-stopped
    volumes:
      - ${DATA}/memos/:/var/opt/memos

networks:
  default:
    external: true
    name: ${DOCKER_NETWORK}
