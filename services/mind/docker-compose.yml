services:
  mind:
    image: mrcas/mind:v1.4.1
    hostname: mind
    container_name: mind
    restart: unless-stopped
    environment:
      - TZ=${TZ}
    volumes:
      - ${DATA}/mind/db:/app/db

networks:
  default:
    external: true
    name: ${DOCKER_NETWORK}
